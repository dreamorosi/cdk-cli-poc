{
  "devDependencies": {
    "@aws-cdk/cli-lib-alpha": "^2.87.0-alpha.0",
    "@aws-sdk/client-dynamodb": "^3.370.0",
    "aws-cdk-lib": "^2.87.0",
    "esbuild": "^0.18.12",
    "typescript": "^5.1.6",
    "vitest": "^0.33.0"
  },
  "scripts": {
    "test": "vitest --run"
  }
}